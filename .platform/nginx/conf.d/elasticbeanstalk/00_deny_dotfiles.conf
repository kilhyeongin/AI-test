# dotfiles 전부 차단 (.git, .env 등)
location ~ /\.(?!well-known).* {
  deny all;
  access_log off;
  log_not_found off;
}

# 특히 .git은 명시적으로 한번 더
location ^~ /.git/ {
  deny all;
  access_log off;
  log_not_found off;
}
